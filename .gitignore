node_modules/
dist/
.env
.env.local

# local dev run logs
dev_out.log
dev_err.log

# 1688 scraper — persisted browser session (contains login cookies) + outputs
scripts/.1688-profile/
scripts/scrape-1688-results.json
.commit-msg.tmp

# Generated reports (regenerate with scripts/build-product-master-xlsx.mjs)
PRODUCT-MASTER.xlsx

# Image pipeline — Gemini staging area (review before publishing to public/)
scripts/image-pipeline/staged/

# Local review artifact (regenerate with scripts/build-blog-review.mjs)
content/blog/REVIEW-blog.html
express-assets/

# sheet-sync secret (local only)
scripts/.sheet-config.json

# social auto-publish secret (local only — Facebook/LINE tokens)
scripts/social/.social-config.json

# pricing scratch
_pricing/

# 1688 raw scrape + restyle work files (large / local only)
scripts/raw-1688/
scripts/_picks/
scripts/_*.json
scripts/_*.txt
scripts/__pycache__/

.vercel
.env*

# ---- เก็บกวาด working tree 2026-08-10 (Golf สั่ง: ไม่ลบไฟล์ ให้ git มองข้ามแทน) ----
# งานทำรูปจริง Express รอบ 2026 — 215MB (ภาพต้นฉบับ/ผลลัพธ์/ล็อก) เก็บไว้บนเครื่อง ไม่เข้า git
express-realphoto-2026/
# รายงานรีวิวที่ฝังรูป base64 ไว้ในไฟล์เดียว ไฟล์ละ 11–33MB — เปิดดูจากเครื่องได้ ไม่ต้องเข้า git
docs/REVIEW-express-*.html
docs/REPORT-express-MASTER.html
# ภาพหน้าจอ QA ที่วางไว้ราก repo + ไฟล์ทดลอง + สำเนาสำรองของ data
/*.png
_cdp-tmp.mjs
src/data/*.bak-*
